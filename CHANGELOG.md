# 更新履歴

## 0.1.9 - 2026-06-17

### 変更

- 送信前のモデル選択が完了しない場合、3秒で送信保持状態を解除するようにしました。
- 狭幅表示の新UIでモデル選択ボタンの表示領域が0サイズになる場合でも、モデル選択を実行できるようにしました。

## 0.1.8 - 2026-06-07

### 変更

- 狭幅表示でモデルボタンが隠れている場合に、右上の三点メニューをより優先して使うようにしました。
- 複数の三点メニュー候補がある画面で、意図しない設定メニューを開きにくくしました。

## 0.1.7 - 2026-05-29

### 変更

- 拡張機能の設定画面を英語UIと日本語UIに対応させました。
- 設定画面に英語/日本語を切り替えるトグルを追加しました。
- `README.md`を英語版にし、`README_JA.md`を日本語版として追加しました。

## 0.1.6 - 2026-05-28

### 変更

- 既定のURL別ルールから`/chat/pages`を削除しました。
- Copilotの送信欄と判断できる入力欄だけで、送信時のモデル選択を行うようにしました。

## 0.1.5 - 2026-05-28

### 変更

- 送信後に入力欄へフォーカスする設定と明示的なフォーカス復帰処理を削除しました。
- 設定保存に失敗した場合は、成功表示ではなくエラーを表示するようにしました。
- 既定のURL別ルールとして、`/chat/pages`と`/chat/agent/new`を含むURLではモデルの自動選択を無効にし、`/chat/agent`を含むURLでは`Think Deeper`を選択するようにしました。
- URL別ルールを上へ/下へボタンで並べ替えできるようにしました。
- 設定画面の変更内容を自動保存するようにし、保存ボタンを削除しました。
- 保存結果の表示を、スクロール位置に影響されない上部固定の通知に変更しました。
- 保存結果の通知が数秒後に自動で消えるようにしました。

## 0.1.4 - 2026-05-28

### 変更

- READMEのインストール手順を、Release zipから展開して読み込む説明に修正しました。
- URL別ルールの説明文を「空にすると、そのURLでは自動選択が無効になります。」に変更しました。

## 0.1.3 - 2026-05-28

### 追加

- 拡張機能名を`MS365 Copilot Auto Model Selector`に変更しました。
- URL別ルールを複数設定できるようにしました。
- 既定のURL別ルールとして、`/agent/`を含むURLでは`Think Deeper`を選択するようにしました。
- `URL別の選択順`を空にすると、そのURLではモデルの自動選択を無効にできるようにしました。

### 変更

- URL別ルールが1行だけの時は、削除ボタンを表示しないようにしました。
- 配布zipのファイル名にバージョンを含めるようにしました。
- 配布zipの中に、拡張機能として読み込めるルートフォルダを入れるようにしました。
